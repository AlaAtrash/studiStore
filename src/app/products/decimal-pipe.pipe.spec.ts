import { DecimalPipePipe } from './decimal-pipe.pipe';

describe('DecimalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
